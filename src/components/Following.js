import React from 'react';

const Following = (props) => {

    return(
        <tr>
            <td>
                <h4>{props.login}</h4>     
            </td>
        </tr>
    );
}
export default Following;