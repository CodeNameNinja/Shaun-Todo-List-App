import React from 'react';

const Gists = (props) => {

    return(
        <tr>
            <td>
                <h4>{props.url}</h4>     
            </td>
        </tr>
    );
}
export default Gists;