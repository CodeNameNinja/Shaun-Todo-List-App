import React from 'react';

const Repos = (props) => {

    return(
        <tr>
            <td>
                <h4>{props.name}</h4>     
            </td>
        </tr>
    );
}
export default Repos;